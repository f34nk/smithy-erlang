{erl_opts, [debug_info, {src_dirs, ["src", "src/generated"]}]}.

{plugins, [rebar3_lint]}.

{deps, [
    {jsx, "3.1.0"}
]}.

{profiles, [
    {test, [
        {deps, [
            {meck, "0.9.2"}
        ]}
    ]}
]}.

{shell, [
    {apps, [jsx]}
]}.

{eunit_opts, [verbose]}.

% {dialyzer, [
%     {plt_apps, all},
%     {warnings, [unknown, race_conditions]}
% ]}.
